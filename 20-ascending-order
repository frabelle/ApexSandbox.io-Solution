public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    //code here
    if (a >= b) {
        if (c >= b ) {
            return true;
        }else {
            return false;
        }
    } else {
        return true;
    }
}